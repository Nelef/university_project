<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.kumoh.ce.university_project_note_ver1">
    <!-- google drive -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.University_project_note_ver1">
        <activity
            android:name=".ui.timeline.MemoSearchActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name=".ui.timeline.TimelineCalendarActivity2"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".ui.timeline.TimelineCalendarActivity" />
        <activity android:name=".ui.timeline.Drive_save_activity" />
        <activity android:name=".ui.timeline.Memo_Input_Activity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.University_project_note_ver1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>